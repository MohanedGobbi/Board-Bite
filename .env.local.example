# Optional. Without this key, the app still works fully — plans are generated by the
# rule-based engine and use template summaries/tips instead of AI-written ones.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
